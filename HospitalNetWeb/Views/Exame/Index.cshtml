@model IEnumerable<HospitalModels.Exame>

@{
    ViewBag.Title = "Exame";
}

<h2>Exames</h2>

<p>
    @Html.ActionLink("Cadastrar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            <label  for="nomeExame">Nome do Exame</label>
            @*            @Html.DisplayNameFor(model => model.NomeExame)*@
        </th>
        <th>
            <label for="observacao">Observação</label>
            @*@Html.DisplayNameFor(model => model.Observacao)*@
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeExame)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observacao)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdExame }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.IdExame }) |
            @Html.ActionLink("Remover", "Delete", new { id=item.IdExame })
        </td>
    </tr>
}

</table>
